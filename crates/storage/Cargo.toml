[package]
name = "storage"
version = "0.1.0"
edition = "2024"

[dependencies]
primitives = { path = "../primitives" }
transaction = { path = "../transaction" }
block = { path = "../block" }
thiserror = "1"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
