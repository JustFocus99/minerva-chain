[package]
name = "execution"
version = "0.1.0"
edition = "2024"

[dependencies]
primitives = { path = "../primitives" }
state = { path = "../state" }
transaction = { path = "../transaction" }
crypto = { path = "../crypto" }