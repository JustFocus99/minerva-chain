//! The root package has no code of its own. It exists only to own
//! `tests/` -- the workspace's cross-crate property and integration tests
//! (Day 5), which don't belong to any single `crates/*` crate. See
//! `readme.md`'s "Repository structure".
